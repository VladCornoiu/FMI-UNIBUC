#include <stdlib.h>
#include <stdio.h>
int main (int argc, char** argv)
{
	if (argc != 3)
		printf("Parameters error");
	else
	{
		int textFile = open(argv[1], O_RDONLY);
		int binaryFile = create(argv[2], 0);
		
		if (textFile == -1)
			printf("Text File does not exist");
		else if (binaryFile == -1)
			printf("Binary File cannot be created");
		else
		{
			while(read(textFile, &character, 1) > 0) 
			{
				
			}
		}
	}
}